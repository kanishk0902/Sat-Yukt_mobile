# CLIENT-SAFE ONLY. This file is bundled into the app JS and readable by anyone
# with the built app. Never put API keys or secrets here — see backend/.env.example.

# Your backend's LAN address, e.g. http://192.168.1.50:4000
EXPO_PUBLIC_BACKEND_URL=http://192.168.1.50:4000
